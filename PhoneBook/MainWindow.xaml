<Window x:Class="PhoneBook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoneBook"
        mc:Ignorable="d"
        Title="MainWindow" Height="355" Width="511">
    <StackPanel Margin="30">
        <Label>Логин:</Label>
        <TextBox Text="{Binding MyPersone.Login, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Label>Пароль:</Label>
        <TextBox Text="{Binding MyPersone.Password, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Button Margin="0,15,0,0" Command="{Binding ShowWindow, UpdateSourceTrigger=PropertyChanged}">Войти</Button>
    </StackPanel>
</Window>
